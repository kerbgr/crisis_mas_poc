"""
Extracted from LLM Training Methodology.
Auto-generated by extract_code_from_methodology.py

Usage example for HuggingFace `evaluate`'s exact_match metric.
"""

from evaluate import load


def main():
    predictions = ["300 ppm", "The IDLH is 500 ppm", "250-300 ppm"]
    references = ["300 ppm", "300 ppm", "300 ppm"]

    metric = load("exact_match")
    results = metric.compute(predictions=predictions, references=references)
    print(results)  # Exact match: 0.33 (only first answer exact match)
    return results


if __name__ == "__main__":
    main()
